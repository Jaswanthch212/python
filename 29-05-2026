class Solution:
    def minElement(self, nums: List[int]) -> int:
        ans = float('inf')

        for num in nums:
            digit_sum = sum(int(d) for d in str(num))
            ans = min(ans, digit_sum)

        return ans
